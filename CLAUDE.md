uvの環境は"~/uv-envs/llamaindex/.venv"の中にあるものを使ってください
作成したmdは"~/Dropbox/obsidian/50_coding/llamaindex"に保存してください
web と web-kimi は同じコードベースのフォーク。明示的に指示されたもの（LLM=Kimi、JA→EN翻訳なし、ポート、表示名）以外は、両方で同じ仕様・同じ実装にする。片方だけ直したら、もう片方にも同じ変更を入れる。
